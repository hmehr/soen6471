Refactor: Move TaskModel.daysBetween() and TaskModel.daysLeft() to a new utility class DateUtil
- Step 1: 
	Create DateUtil class
	Copy TaskModel.daysBetween() and TaskModel.daysLeft() there
	
- Step 2:
	Replace all usage of TaskModel.daysBetween() and TaskModel.daysLeft()
	Run unit tests if possible
	
- Step 3:
	Remove TaskModel.daysBetween() and TaskModel.daysLeft()
	Run unit tests if possible